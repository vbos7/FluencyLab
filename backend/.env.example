# Não use este arquivo. A configuração fica no .env da RAIZ do projeto.
#
# Veja ../.env.example — é de lá que o backend/api/env.php lê quando você
# roda `php -S` no terminal.
#
# Este arquivo continua aqui só para não confundir quem procurar um .env
# dentro de backend/.
